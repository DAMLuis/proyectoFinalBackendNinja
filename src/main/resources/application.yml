spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mydb?useSSL=false
    username: root
    password: toor
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto:  update

    properties:
      hibernate: 
        dialect: org.hibernate.dialect.MySQL5Dialect
        
      